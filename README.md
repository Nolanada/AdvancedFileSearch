Below are the requirements for this app to get started
sentence-transformers
pip install sentence-transformers

torch
pip install torch

scikit-learn
pip install scikit-learn

numpy
pip install numpy

joblib
pip install joblib

PyPDF2
pip install PyPDF2

python-docx
pip install python-docx

pandas
pip install pandas

transformers
pip install transformers

huggingface_hub
pip install huggingface_hub
